server:
  port: 80
spring:
  application:
    name: consul-consumer-order
  ##consul 注册中心
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}